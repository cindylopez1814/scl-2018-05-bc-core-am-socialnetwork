describe('', () => {
  it('', () => {
    assert.isFunction();
  });
  it('', () => {
    assert.isFunction();
  });
  it('', () => {
    assert.isFunction();
  });
  it('', () => {
    assert.isFunction();
  });
});